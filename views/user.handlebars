<main class="container mb-5">

    <div class="row mb-3">

        <div class="col-md-12 text-center mt-5">
            <h1>My Account</h1>
            <p class="alert">
                <i>This is an Alert! This is an Alert! This is an Alert! This is an Alert!</i>
            </p>
            <hr class="mt-3">

        </div>
    </div>

    <div class="row">

        <div class="col-md-4 text-center profile-img">
            <h3>{{this.userData.firstName}} {{this.userData.middleName}} {{this.userData.lastName}}</h3>
            <p>Member since:
                <span>
                    1996
                </span>
            </p>
            <img src="{{this.userData.profilePicture}}" class="img-fluid" alt="I am a profile picture">
            <div class="row">

                <div class="col-md-12 text-left mt-5">

                    <p>
                        <i class="fa fa-book" aria-hidden="true"></i>
                        <span>LIB-730-00{{this.userData.id}}</span>
                    </p>
                    <p>
                        <i class="fa fa-envelope-o" aria-hidden="true"></i>
                        <span>{{this.userData.emailAddress}}</span>
                    </p>
                    <p>
                        <i class="fa fa-phone" aria-hidden="true"></i>
                        <span>{{this.userData.phoneNumber}}</span>
                    </p>
                    <p>
                        <a class="btn btn-success text-uppercase max-with" href="/">Reserve a Book</a>
                    </p>

                </div>
            </div>
        </div>
        <div class="col-md-8 border-lv">
            <ul class="nav nav-tabs" id="myTab" role="tablist">
                <li class="nav-item">
                    <a class="nav-link active" id="myBooks-tab" data-toggle="tab" href="#myBooks" role="tab" aria-controls="myBooks" aria-selected="true">My Books</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="history-tab" data-toggle="tab" href="#history" role="tab" aria-controls="history" aria-selected="false">Check-Out History</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="favorites-tab" data-toggle="tab" href="#favorites" role="tab" aria-controls="favorites" aria-selected="false">Favorites</a>
                </li>
            </ul>
            <div class="tab-content" id="myTabContent">
                <div class="tab-pane fade show active" id="myBooks" role="tabpanel" aria-labelledby="myBooks-tab">
                    {{#each reservations}}
                    <article class="card mt-3">
                        <div class="card-body row">
                            <div class="col-md-11">
                                <h4 class="card-title">{{this.Medium.title}}</h4>
                                <h6 class="card-subtitle mb-2 text-muted">by
                                    <span>
                                        <a href="#">{{this.Medium.author}}</a>
                                    </span>
                                </h6>
                                <p>
                                    <b class="text-uppercase">Status: </b>
                                    <span class="alert">
                                        <i> {{this.reservationStatus}}</i>
                                    </span>
                                </p>
                                <button id="actionBtnReserve" class="btn btn-success text-uppercase" type="button" data-medium-id="{{this.Medium.id}}" data-user-id="{{../userData.id}}">
                                    Cancel Book
                                </button>

                            </div>
                            <div class="col-md-1">
                                <span class="favorite-book actionBtnFav" data-medium-id="{{this.Medium.id}}" data-user-id="{{../userData.id}}">
                                    <i class="fa fa-heart-o " aria-hidden="true "></i>
                                </span>
                            </div>
                        </div>
                        <hr>
                    </article>
                    {{/each}}
                </div>

                <div class="tab-pane fade " id="history " role="tabpanel " aria-labelledby="history-tab ">
                    {{#each checkOutHistories}}
                    <article class="card mt-3 ">
                        <div class="card-body row ">
                            <div class="col-md-11 ">
                                <h4 class="card-title ">{{this.Medium.title}}</h4>
                                <h6 class="card-subtitle mb-2 text-muted ">by
                                    <span>
                                        <a href="# ">{{this.Medium.author}}</a>
                                    </span>
                                </h6>
                                {{!--
                                <div class="checkbox "> --}} {{!--
                                    <label>
                                        <input type="checkbox " value=" "> --}}
                                        <p>
                                            <b class="text-uppercase ">Book Status: </b>
                                            <span>Reserved / Waitlist
                                                <button type="button " class="close " data-dismiss="modal
                                    " aria-label="Close ">
                                                    <span aria-hidden="true ">×</span>
                                                </button>
                                            </span>
                                        </p> {{!--
                                        <span>Return by December 15, 2017</span> --}} {{!-- </span>
                                    </label>
                                </div> --}} {{!--
                                <div class="checkbox ">
                                    <label>
                                        <input type="checkbox " value=" ">
                                        <span>Reserved
                                            <span>5</span>
                                        </span>
                                    </label>
                                </div> --}}
                            </div>
                            <div class="col-md-1 ">
                                <span class="favorite-book ">
                                    <i class="fa fa-heart-o " aria-hidden="true "></i>
                                </span>
                            </div>
                        </div>
                    </article>
                    {{/each}}
                </div>
                <div class="tab-pane fade " id="favorites " role="tabpanel " aria-labelledby="favorites-tab ">
                    {{#each favorites}}
                    <article class="card mt-3 ">
                        <div class="card-body row ">
                            <div class="col-md-11 ">
                                <h4 class="card-title">{{this.Medium.title}}</h4>
                                <h6 class="card-subtitle mb-2 text-muted ">by
                                    <span>
                                        <a href="# ">{{this.Medium.author}}</a>
                                    </span>
                                </h6>
                                <p>
                                    <b class="text-uppercase ">Status: </b>
                                    <span class="alert ">
                                        <i> {{this.reservationStatus}}</i>
                                    </span>
                                </p>
                                <button id="actionBtnReserve " class="btn btn-success text-uppercase " type="button " data-medium-id="{{this.Medium.id}}"
                                    data-user-id="{{../userData.id}}">
                                    Cancel Book
                                </button>

                            </div>
                            <div class="col-md-1 ">
                                <span class="favorite-book actionBtnFav " data-medium-id="{{this.Medium.id}}" data-user-id="{{../userData.id}}">
                                    <i class="fa fa-heart-o " aria-hidden="true "></i>
                                </span>
                            </div>
                        </div>
                        jsdhfgdsjhflds
                        <hr>
                    </article>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
</main>