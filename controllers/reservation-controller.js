var db = require("../models");
//require mediaUpdateExport
//this has the mediaupdate and the media delete functions

module.exports = app => {
	//CREATE A RESERVATION
	//POST ROUTE to reservation table
	//need to give media id and user id
	//.then
	//edit media table
	//.then
	//maybe send back the position in reservation table
	//DELETE A RESERVATION
	//DELETE to reservations route
	//.then
	//update media table
};
